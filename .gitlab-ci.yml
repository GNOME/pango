image: registry.gitlab.gnome.org/gnome/gnome-runtime-images/base:latest

build:
  script:
    - dnf install -y gcc meson
    - meson _build
    - ninja -C _build dist
  artifacts:
    paths:
      - build/meson-dist
    expire_in: 7 days
